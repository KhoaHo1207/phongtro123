// luu nhung thong tin nguoi dung

const iniState = {
  userData: {},
};

const userReducer = (state = iniState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
//actionb nhan thong ham dispatch

export default userReducer;
